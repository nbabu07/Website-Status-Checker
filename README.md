# Website-Status-Checker
# Python Site Status Checker

A simple tool to monitor the uptime of your favorite websites.

## Requirements
You'll need the `requests` library:
`pip install requests`

## Usage
Add your URLs to the `websites` list in `site_checker.py` and run the script!
